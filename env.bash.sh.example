# Example env file for Git Bash / bash-like shells.
#
# Usage:
#   cp env.bash.sh.example env.bash.sh
#   # edit env.bash.sh to set your real API key
#   source ./env.bash.sh
#
# WARNING: Never commit env.bash.sh (it can contain secrets).

export PHPDECIDE_AI_API_KEY="YOUR_API_KEY_HERE"

# Pick a model. Examples:
# - OpenAI: gpt-4o-mini
# - OpenRouter: deepseek/deepseek-chat-v3-0324
export PHPDECIDE_AI_MODEL="gpt-4o-mini"

# Base URL for OpenAI-compatible Chat Completions:
# - OpenAI: https://api.openai.com
# - OpenRouter: https://openrouter.ai/api
# - DIAL: your DIAL gateway base URL
export PHPDECIDE_AI_BASE_URL="https://api.openai.com"

# If your gateway uses a non-standard OpenAI-compatible path, override it.
# Example (DIAL deployments may include the model/deployment in the URL):
# export PHPDECIDE_AI_CHAT_COMPLETIONS_PATH="/openai/deployments/<deployment>/chat/completions"

# If your gateway encodes the model in the URL path and rejects a JSON "model" field,
# set this to true and leave PHPDECIDE_AI_MODEL as any value (it will be omitted).
# export PHPDECIDE_AI_OMIT_MODEL="true"

# Auth header customization (some gateways reject "Authorization: Bearer ...").
# Examples:
# - Default (Bearer token):
#   export PHPDECIDE_AI_AUTH_HEADER_NAME="Authorization"
#   export PHPDECIDE_AI_AUTH_PREFIX="Bearer"
# - Azure/OpenAI-proxy style:
#   export PHPDECIDE_AI_AUTH_HEADER_NAME="Api-Key"
#   export PHPDECIDE_AI_AUTH_PREFIX=""

# Timeout in seconds (optional)
export PHPDECIDE_AI_TIMEOUT="20"

# Optional: point to a CA bundle if you hit cURL SSL errors on Windows.
# curl -L -o docs/cacert.pem https://curl.se/ca/cacert.pem
# WIN_CA="$(pwd -W)\\docs\\cacert.pem"
# export PHPDECIDE_AI_CAINFO="$WIN_CA"

# Escape hatch (NOT recommended): skip TLS verification.
# export PHPDECIDE_AI_INSECURE="true"
